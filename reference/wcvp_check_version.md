# Check if the packaged version of WCVP is up to date.

Check if the packaged version of WCVP is up to date.

## Usage

``` r
wcvp_check_version(silent = FALSE)
```

## Arguments

- silent:

  Raise a warning if the version is out of date.

## Examples

``` r
wcvp_check_version()
#> [1] TRUE
```
